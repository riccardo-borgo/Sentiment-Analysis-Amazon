[project]
name = "sentiment-analysis-amazon"
version = "0.1.0"
description = ""
authors = [
    {name = "riccardo-borgo",email = "riccardo.borgo00@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.10,<4.0"
dependencies = [
    "pandas (>=2.3.3,<3.0.0)",
    "logging (>=0.4.9.6,<0.5.0.0)",
    "nltk (>=3.9.2,<4.0.0)",
    "transformers (>=4.57.0,<5.0.0)"
]

[tool.poetry]
packages = [
  { include = "sentiment_analysis_amazon", from="."}
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "commitizen (>=4.9.1,<5.0.0)"
]
